% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db.R
\name{ch_connect}
\alias{ch_connect}
\title{ClickHouse persistence layer}
\usage{
ch_connect(
  host = Sys.getenv("CH_HOST", "localhost"),
  port = as.integer(Sys.getenv("CH_PORT", "9000")),
  dbname = Sys.getenv("CH_DB", "ttrss"),
  user = Sys.getenv("CH_USER", "default"),
  password = Sys.getenv("CH_PASSWORD", "")
)
}
\arguments{
\item{host}{ClickHouse host       (env: \code{CH_HOST}, default \code{localhost}).}

\item{port}{Native-protocol port  (env: \code{CH_PORT}, default \code{9000}).}

\item{dbname}{Database name         (env: \code{CH_DB},   default \code{ttrss}).}

\item{user}{Username              (env: \code{CH_USER}, default \code{default}).}

\item{password}{Password              (env: \code{CH_PASSWORD}, default \code{""}).}
}
\value{
A \code{DBIConnection} object.
}
\description{
Functions to initialise schema, write articles and feeds,
and read analytical views from ClickHouse.
Open a ClickHouse connection

Connection parameters are read from environment variables when arguments
are omitted, making it easy to configure via \code{.env} / Docker env.
}
