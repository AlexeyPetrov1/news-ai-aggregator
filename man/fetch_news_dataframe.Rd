% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/etl.R
\name{fetch_news_dataframe}
\alias{fetch_news_dataframe}
\title{ETL: collect TT-RSS articles into a unified data frame}
\usage{
fetch_news_dataframe(
  base_url,
  user,
  password,
  max_articles = 1000L,
  batch_size = 200L,
  since_id = 0L
)
}
\arguments{
\item{base_url}{TT-RSS base URL, e.g. \code{"http://localhost:8080"}.}

\item{user}{TT-RSS username.}

\item{password}{TT-RSS password.}

\item{max_articles}{Maximum total articles to retrieve.}

\item{batch_size}{Articles per API call (capped at 200).}

\item{since_id}{Only return articles with ID greater than this value.
Pass \code{0} (default) to fetch everything.}
}
\value{
A data frame with one row per article and columns:
\code{article_id}, \code{title}, \code{content}, \code{content_text},
\code{link}, \code{feed_id}, \code{feed_title}, \code{author},
\code{published_at}, \code{fetched_at}, \code{is_unread}, \code{is_starred}.
}
\description{
High-level functions that orchestrate login → paginated fetch
→ normalisation → logout in one call.
Fetch all articles from TT-RSS and return a tidy data frame

The function pages through the \emph{all articles} virtual feed
(\code{feed_id = -4}) in batches of up to 200 items until either
\code{max_articles} is reached or the API returns fewer items than
requested (i.e. end-of-feed).
}
