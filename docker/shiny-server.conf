run_as shiny;

server {
  listen 3838;

  location /ttrss {
    app_dir  /srv/shiny-server/ttrss/shiny;
    log_dir  /var/log/shiny-server;
    directory_index on;
  }

  location / {
    site_dir /srv/shiny-server;
    log_dir  /var/log/shiny-server;
    directory_index on;
  }
}
